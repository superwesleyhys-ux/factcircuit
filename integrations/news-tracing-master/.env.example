# Optional: only used with the explicit --tunnel api legacy workflow.
OPENAI_API_KEY=sk-xxx
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=provider-model-id
OPENAI_SEARCH_MODEL=provider-search-model-id

# Optional project default for the local FactCircuit route.
FACTCIRCUIT_MODEL=gpt-6-astra
